apiVersion: v1
kind: Service
metadata:
  name: authorization-service-postgres
  labels:
    app: dottit
    service: authorization-service
spec:
  type: ClusterIP
  selector:
    app: authorization-service-postgres-pod
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
