apiVersion: v1
kind: Service
metadata:
  name: thread-service-mysql
  labels:
    app: dottit
    service: thread-service
spec:
  type: ClusterIP
  selector:
    app: thread-service-mysql-pod
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
